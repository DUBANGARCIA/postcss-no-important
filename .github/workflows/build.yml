name: Build
on:
  push:
    branches:
      - main
  pull_request:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout
          uses: actions/checkout@44c2b7a8a4ea60a981eaca3cf939b5f4305c123b # v4
        - name: Set up Tools
          uses: jdx/mise-action@0c39a522dfbced3ed83924152c45804a71ce216f # v2
          with:
            install: true
            cache: true
        - name: Install dependencies
          run: pnpm install --ignore-scripts
